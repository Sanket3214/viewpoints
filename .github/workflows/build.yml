name: Build DLL

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x

    - name: Restore
      run: dotnet restore PRYANIK_Plugin.sln

    - name: Build
      run: dotnet build PRYANIK_Plugin.sln --configuration Release

    - name: Upload DLL
      uses: actions/upload-artifact@v4
      with:
        name: pryanik-dll
        path: |
          **/bin/Release/*.dll
